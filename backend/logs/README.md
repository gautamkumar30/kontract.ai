# Logs Directory

This directory contains application logs:

- `app.log` - All application logs
- `errors.log` - Error logs only

Logs are automatically rotated when they reach 10MB (keeps 5 backups).
